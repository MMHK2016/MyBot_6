* **V6.1.4.MMHK.v6.6.2**
 * Upgrade MyBot v6.1.4
 * Add GTFO - Dedicated donation; Kick NEW members donated or received more than 35 or from last page of list; Adjustable frequencies
 * Improve Smart chat donation - stay at first page wait for new request after scroll
 * Improve Smart chat donation - learning ability, limited saved req images, compare backward
 * Fix Traditional Chinese, Hong Kong system language detection
 * Fix GTFO kick minutes not accurate
 * Fix GTFO interval GUI change back to index 1 error when start bot
 * Update Chinese.ini
 * Move mod codes under a clean MOD directory - OCR

* **V6.1.3.MMHK.v6.5.2**
 * Add "Dumb Zap" option - zap any drills, trigger limited by total DE available to be stolen (in settings), use it when you don't care about elixir
 * Fix smart zap gain 0 error - actually did not drop lightnings

* **V6.1.3.MMHK.v6.5.0**
 * Upgrade MyBot v6.1.3
 * Add Donation limit (per round)
 * Add Enable localization
 * Improve Smart chat donation - fast search, donate downward
 * Improve Smart chat donation - handle requests more than one page while avoiding checking same requests repeatedly (per round)
 * Fix Taiwanese language detection

* **V6.1.2.1.MMHK.v6.4.2**
 * Improve Now should collect all elixir and dark elixir thx to @trlopes

* **V6.1.2.1.MMHK.v6.4.1**
 * Fix collectors outside duplicated logs
 * Fix Request First pattern

* **V6.1.2.1.MMHK.v6.4.0**
 * Add Option to Check Collectors Outside thks to @McSlither
 * Add Option to Request First - move the Request CC Troops function to the beginning of the run loop
 * Improve Don't empty barracks - for Barrack Mode only - default build-in, not an option
 * Improve Smart chat donation - faster search backward, donation upward, limit to top req only after scrolling up
 * Improve Minimize the emulater after restart - preserve the state of the emulater before planned close
 * Fix "No collectors are selected" warning error - the checkbox wont be allowed unchecked if all others are unchecked
 * Fix aqua color 0 shown up in log window
 * Update mmhk mod log messages to color fuchsia, leave purple to debug
 * Start moving all mod codes into a clean MOD directory

* **V6.1.2.1.MMHK.v6.3.1**
 * Fix Smart Zap DE Stats disabled
 * Fix Smart Zap description enabled when first started

* **V6.1.2.1.MMHK.v6.3.0**
 * Upgrade MyBot v6.1.2.1
 * Add Smart Zap thks to @LunaEclipse, @cutidudz
 * Improve match found tray tip with profile name and search # displayed
 * Fix mod version label then discard it
 * Update all close message to color purple
 * Update Mod Title

* **V6.1.1.MMHK.v6.2.0**
 * Add offline when training - multiple times randomly and options to
		only sleep if longer than some minutes,
		disconnected naturally sometimes,
		max extra offline time, randomly,
		incoporates Full Camp percentage
 * Fix problem clicking donate button in the clan chat
 * Update Mod Title
 * Update Logo
 * Credit goes to @ProMac providing RemainTrainTime listSymbols

* **V6.1.1.MMHK.v6.1.0**
 * Add Close the emulator according to Attack Schedular and random extra minutes
 * Fix encoding from western 1252 to UTF-8 or not compile
 * Fix wrong array size PlannedAttack
 * Fix same array save config wrongly - an extra delimiter - all hours planned section
 * Fix read config wrong default - all hours planned section
 * Fix wrong apply config size
 * Update Mod Title
